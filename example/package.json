{
  "name": "selfjs-react-example",
  "version": "0.0.0",
  "description": "Universal React app served by a Go server",
  "scripts": {
    "build": "rm -rf build && mkdir build && babel universal.js --out-file build/universal.js && browserify build/universal.js --outfile build/bundle.js"
  },
  "dependencies": {
    "react": "^0.13.3",
    "react-router": "^0.13.3"
  },
  "devDependencies": {
    "babel": "^5.6.14",
    "browserify": "^10.2.4"
  }
}
